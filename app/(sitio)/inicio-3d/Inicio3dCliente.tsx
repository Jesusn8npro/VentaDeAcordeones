'use client'

/**
 * INICIO 3D — lienzo totalmente en blanco.
 * Sin encabezado, sin pie de página, sin flotantes (excluido en ArmazonGlobal).
 * A partir de aquí se construye desde cero con los prompts nuevos + acordeón 3D.
 */
export default function Inicio3dCliente() {
  return (
    <main
      style={{
        minHeight: '100vh',
        width: '100%',
        background: '#000',
      }}
    >
      {/* Lienzo en blanco — el contenido nuevo se agrega aquí */}
    </main>
  )
}
